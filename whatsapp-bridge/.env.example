# Port the sidecar listens on. Must match WHATSAPP_BRIDGE_URL in mesync/.env
PORT=3011

# Only allow requests from loopback (anything else 403s). Change to 0 to disable the check.
ALLOW_LOOPBACK_ONLY=1
