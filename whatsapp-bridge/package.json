{
  "name": "mesync-whatsapp-bridge",
  "version": "0.1.0",
  "description": "WhatsApp sidecar for MeSync — exports recent chats over HTTP so Flask can ingest them.",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "qrcode": "^1.5.4",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.25.0"
  }
}
